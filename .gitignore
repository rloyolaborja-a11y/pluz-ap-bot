# Claves/config -- NUNCA se suben (viven solo en cada PC)
config.json
credentials-pluz
*.pat

# Preferencia de motor de navegador para SAP -- es POR PC (cada una puede
# andar mejor con uno distinto), no se comparte entre maquinas.
RPA-SAP-LOCAL/motor_navegador.txt

# El sitio web ya es su propio repo aparte (PUBLICAR-WEB-LOCAL/SITIO)
PUBLICAR-WEB-LOCAL/SITIO/

# El clon local del repo de datos (pluz-ap-datos) es su propio repo git
# aparte tambien -- cada PC lo clona una vez (ver DATOS-GITHUB-LOCAL/LEEME.txt).
DATOS-GITHUB-LOCAL/pluz-ap-datos/

# Datos descargados/generados -- pesan mucho y no son "código"
CARGA/
**/SALIDA/
**/HISTORICO/
**/EXCEL-MESES-ANTERIORES/
PENDIENTES-LOCAL/HISTORICO-NODT/
**/logs/
**/runs/
**/_diagnosticos/
PANEL-CONTROL-LOCAL/_panel.pid
PANEL-CONTROL-LOCAL/_clic_fantasma.pid
PANEL-CONTROL-LOCAL/agenda_estado.json
PANEL-CONTROL-LOCAL/tick.log
PANEL-CONTROL-LOCAL/tick_latido.json
PANEL-CONTROL-LOCAL/o.txt
*.xlsx
*.xls
*.xlsm

# Perfil del navegador embebido del panel (cookies/sesión -- nunca subir)
**/.ventana-app/

# Python
__pycache__/
*.pyc

# Windows / editor
.DS_Store
Thumbs.db
NUL
